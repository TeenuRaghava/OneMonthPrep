def plusMinus(arr):
    # Write your code here
    p=0
    n=0
    z=0
    for i in range(len(arr)):
        if(arr[i]>0):
            p=p+1
        elif(arr[i]<0):
            n=n+1
        else:
            z+=1
    print(p/len(arr))
    print(n/len(arr))
    print(z/len(arr))
